test('register get route', () => {
    // 'register' template is rendered
});

test('register post route', () => {
    // user is created
});

test('login get route', () => {
    // 'login' template is rendered
});

test('login post route', () => {
    // user is authenticated and redirected to '/'
});

test('logout route', () => {
    // user is logged out
});
